import React from 'react';
import ReactDOM from 'react-dom';
import { TareasApp } from './07-useReducer/TareasApp';

ReactDOM.render(
  <React.StrictMode>
    <TareasApp />
  </React.StrictMode>,
  document.getElementById('root')
);
